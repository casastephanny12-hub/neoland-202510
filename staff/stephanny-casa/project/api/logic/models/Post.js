export class Post{
    constructor(id, ownerId, text, url, postedAt){
        this.id = id
        this.ownerId = ownerId
        this.text = text
        this.url = url
        this.postedAt = postedAt
    }
}

