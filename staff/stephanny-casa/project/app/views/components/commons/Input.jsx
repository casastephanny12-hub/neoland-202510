export function Input ({alias, autoComplete, type, className, defaultValue}) {
return <input id={alias} name={alias} autoComplete={autoComplete || alias } type={type} className={`border border-gray-400 py-1 rounded-xl  ${className}`} defaultValue={defaultValue} />}